*/1 * * * * root /usr/bin/env bash -c 'if [[ $(/usr/local/bin/kubectl get pods -n kube-system | grep CrashLoopBackOff | wc -l) -ge 1 ]]; then for i in $(seq 1 20); do systemctl restart k3s; sleep 30; done; fi;' >> /var/log/restart-k3s.log 2>&1
